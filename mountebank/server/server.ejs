{
    "port": 5055,
    "protocol": "https",
    "name": "origin",
    "stubs": [
      {
        "predicates": [
          {
            "equals": {
              "method": "GET",
              "path": "/api/character"
            }
          }
        ],
        "responses": [
          {
            "inject": "<%- stringify(filename, '../settings/server_response.ejs') %>"
          }
        ]
      },
      {
        "predicates": [{ "contains": { "headers": { "Content-Type": "xml" } } }],
        "responses": [{ "is": { "body": "<%- stringify(filename, '../settings/xml_response.ejs') %>" }}]
      }
    ]
}